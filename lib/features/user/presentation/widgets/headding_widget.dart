import 'package:flutter/material.dart';

class HeaddingWidget extends StatelessWidget {
  const HeaddingWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('All Users'),
    );
  }
}
